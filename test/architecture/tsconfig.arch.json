{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "isolatedModules": true
  },
  "include": ["../../src/**/*.ts", "../../scripts/**/*.ts"],
  "exclude": [
    "../../node_modules",
    "../../dist",
    "..",
    "../../coverage",
    "../../.gemini"
  ]
}
