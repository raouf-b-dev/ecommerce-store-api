current_branch=$(git rev-parse --abbrev-ref HEAD)

if [ "$current_branch" = "master" ] || [ "$current_branch" = "develop" ]; then
  echo "❌ Error: Direct commits to '$current_branch' are not allowed. Please use a feature branch and create a Pull Request."
  exit 1
fi

npx lint-staged
