NODE_ENV=example
PORT=3000
APP_VERSION=""

REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=secret
REDIS_KEYPREFIX=ecom:example:
REDIS_DB=0

# DB CONFIG // Warning: Turn this off in production
IS_DB_SYNCHRONIZE=false

JWT_PRIVATE_KEY="<paste-your-rsa-pem>"
JWT_ACCESS_TOKEN_TTL=15m
JWT_REFRESH_TOKEN_TTL=7d

DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_password
DB_DATABASE=my_database
POSTGRES_CONTAINER_NAME=postgres-db
POSTGRES_IMAGE=postgres:18.4

CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:5173,http://localhost:5174

# Logging
LOG_LEVEL=info
LOG_DIR=./logs
LOG_TRANSPORT=both

# Rate Limiting (Throttler)
THROTTLE_GLOBAL_LIMIT=100
THROTTLE_STRICT_LIMIT=10

# Reverse proxy hop count / trust (false | true | 1 | 2 | subnet list)
TRUST_PROXY=false

# Metrics
METRICS_API_KEY=super-secret-metrics-key
# If PORT is remapped for a host-run API, update docker/monitoring/prometheus/prometheus.yml to the same host port.
# Prometheus publishes to the host via PROMETHEUS_HOST_PORT when the observability profile is enabled.
PROMETHEUS_HOST_PORT=9090

# OpenTelemetry
OTEL_TRACING_ENABLED=true
# If OTLP_GRPC_HOST_PORT is remapped, this endpoint must match because npm run start:dev runs on the host.
OTEL_EXPORTER_OTLP_ENDPOINT=http://localhost:4317
OTLP_GRPC_HOST_PORT=4317
OTLP_HTTP_HOST_PORT=4318
TEMPO_HOST_PORT=3200

# Grafana (Monitoring Stack)
GRAFANA_ADMIN_USER=admin_user
GRAFANA_ADMIN_PASSWORD=secure_password
# Optional host publish remaps (needed on some Windows hosts where 3000-3199 are reserved)
GRAFANA_HOST_PORT=3001
LOKI_HOST_PORT=3100
